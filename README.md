# PhenoContracts

PhenoContracts: shared Phenotype API contracts

> Scaffolded repo. Sections below are minimal placeholders — replace with
> real content as the project matures.

## Build

Replace this section with the project's build command (e.g. `cargo build`,
`npm run build`, `go build ./...`, `pip install -e .`).

## Test

Replace this section with the project's test command (e.g. `cargo test`,
`npm test`, `go test ./...`, `pytest`).

## License

MIT — see [LICENSE](LICENSE).
